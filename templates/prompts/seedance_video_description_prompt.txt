你是广告导演，请基于以下素材与要求，生成一条视频制作需求（仅描述创意与画面，不写任何工具调用步骤）：

- 素材包ID：{{asset_package_id}}
- 可用素材：{{references_csv}}
- 时长：{{duration_seconds}} 秒
- 画幅：{{ratio}}
- 风格：{{style_keywords}}
- 核心卖点：{{core_selling_points}}
- 目标受众：{{target_audience}}

素材引用约定：在描述中使用 `@文件名` 引用素材，例如 `@intro.mp4`、`@cover.jpg`、`@bgm.mp3`。

输出要求：
1) 只输出“视频描述正文”，不要输出步骤、JSON、代码块。
2) 描述包含：开场、主体推进、节奏、镜头语言、结尾记忆点。
3) 保持可执行与可拍摄，避免抽象空话。
