# Seedance 素材包视频生成 v1.0 开发 TODO

## 1. 里程碑

- M1：稳定可用（主链路 100% 可跑通）
- M2：工程化（部署、回归、日志、脚本自动化）
- M3：体验优化（提示词与 Skill 一致性、使用手册稳定输出）

## 2. 当前进度总览（2026-04-24）

### 已完成

- [x] 后端 `material-packages` 路由集已实现并可加载。
- [x] Tool v2 单一导入文件策略已落实。
- [x] 聊天上传入包接口：`/from-upload` 已上线。
- [x] 单文件成包与多单文件合并成包规则已实现。
- [x] 包名与包 ID 语义拆分已落地（`package_display_name` vs `asset_package_id`）。
- [x] 包内素材地址查询已实现（含 `tos_key/tos_status`，可选 `temp_url`）。
- [x] 生成任务管理（列表/详情/等待）已实现。
- [x] 服务器 conda + systemd 部署链路已跑通。
- [x] HF 离线缓存方案已验证可启动。

### 进行中

- [ ] from-upload 在不同 OpenWebUI 版本下的兼容回归（异步/同步调用差异）。
- [ ] 补丁同步脚本自动注入 `main.py` router 注册，避免人工补丁。

### 待完成（v1.0 收口）

- [ ] 建立最小回归脚本：上传 -> resolve -> generate -> task 查询。
- [ ] 统一本地与服务器脚本默认端口策略（8080/8801）。
- [ ] 统一代理环境下脚本行为（默认 `--noproxy` 或显式 `NO_PROXY` 提示）。
- [ ] 统一用户帮助输出（固定模板，避免模型自由发挥）。

## 3. P0（阻断项）

- [ ] 解决 `create_material_package_from_chat_upload` 的版本兼容问题并完成线上回归。
- [ ] 将补丁同步流程固化为“一条命令完成”（router + main + restart 前检查）。

## 4. P1（高优先）

- [ ] 新增 `scripts/smoke_material_flow.sh`：
  - [ ] 上传 ZIP
  - [ ] 列表/详情
  - [ ] resolve
  - [ ] generate
  - [ ] task polling
- [ ] 运行命令手册增加“服务器标准操作卡（conda）”。
- [ ] check 脚本对代理环境做默认兼容（localhost 永不走代理）。

## 5. P2（中优先）

- [ ] Tool 返回字段与 Skill 模板做一次严格对齐检查。
- [ ] 用户手册补齐“异常处理话术与命令”最小集。
- [ ] 增加任务失败样例集（便于 LLM 准确回报错误）。

## 6. 发布前检查清单（Release Checklist）

- [ ] `check_material_routes.sh` 在目标端口返回完整路由。
- [ ] Tool 导入文件与服务端能力一致。
- [ ] 样例素材包可成功生成并返回可访问视频 URL。
- [ ] 失败路径返回结构化错误字段完整。
- [ ] 部署文档按新路径 `openwebui-seedance` 全量可执行。

## 7. 验收标准（v1.0）

- [ ] 用户从聊天上传素材开始，无需手工改后端即可跑通到视频结果。
- [ ] 任务状态可追踪，结果可复查。
- [ ] 脚本与文档在本地/服务器两端一致可执行。
- [ ] 已知高频报错有明确排障路径。
